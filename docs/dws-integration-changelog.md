# 钉钉 CLI 集成更新说明

## 版本：2026.03.28

### 新增功能

✅ **钉钉 CLI (dws) 完整集成**

现在可以通过 `openyida dws` 命令直接使用钉钉官方 CLI 工具的所有功能，包括：

- 📇 **通讯录管理** - 用户/部门搜索与管理
- 📅 **日历日程** - 事件创建、闲忙查询、会议室预定
- ✅ **待办任务** - 任务创建、分配、跟踪
- 📝 **审批流程** - 表单管理、实例创建与查询
- 🕐 **考勤打卡** - 排班、统计、打卡记录查询
- 💬 **群聊机器人** - 群成员管理、机器人消息发送
- 📊 **智能表格** - AI 表格操作
- 🔔 **DING 消息** - 发送与撤回
- 📋 **日志管理** - 模版与统计
- 🖥️ **工作台** - 应用查询
- 📚 **开发者文档** - 开放平台文档搜索

### 新增文件

#### 1. 核心模块
- `lib/dws/dws-wrapper.js` - 钉钉 CLI 包装器（262 行）
  - 自动检测 dws 安装状态
  - 提供一键安装功能
  - 透传所有 dws 命令
  - 友好的安装指引

#### 2. 测试文件
- `tests/dws-integration.test.js` - 集成测试（65 行）
  - 验证命令帮助信息
  - 验证主帮助集成
  - 验证示例命令显示

#### 3. 文档
- `docs/dws-cli-guide.md` - 完整使用指南（208 行）
  - 安装说明
  - 配置步骤
  - 所有支持的服务列表
  - 输出格式说明
  - 环境变量配置
  - 常见问题解答

- `docs/dws-quick-start.md` - 快速开始指南（269 行）
  - 5 分钟上手教程
  - 常用场景示例
  - AI Agent 集成示例
  - 调试技巧
  - 实用小技巧

### 修改内容

#### bin/yida.js
- ✅ 添加 `dws` 命令路由
- ✅ 更新帮助文档，包含 dws 命令说明
- ✅ 添加使用示例到帮助信息

### 使用方法

#### 查看帮助
```bash
openyida dws --help
```

#### 安装钉钉 CLI
```bash
openyida dws install
```

#### 使用钉钉 API
```bash
# 搜索联系人
openyida dws contact user search --keyword "悟空"

# 创建待办
openyida dws todo task create --title "任务" --executors "userId"

# 列出日历事件
openyida dws calendar event list
```

### 技术实现

#### 架构设计
```
┌─────────────────────────────────────┐
│   OpenYida (bin/yida.js)           │
│   ┌─────────────────────────────┐  │
│   │ dws 命令路由                │  │
│   └──────────┬──────────────────┘  │
│              │                      │
│   ┌──────────▼──────────────────┐  │
│   │ lib/dws/dws-wrapper.js      │  │
│   │ • 安装检测                  │  │
│   │ • 自动安装                  │  │
│   │ • 命令透传                  │  │
│   └──────────┬──────────────────┘  │
└──────────────┼─────────────────────┘
               │
               │ spawn
               ▼
┌─────────────────────────────────────┐
│   系统安装的 dws (Go 二进制)        │
│   • 通讯录服务                      │
│   • 日历服务                        │
│   • 待办服务                        │
│   • 审批服务                        │
│   • 考勤服务                        │
│   • ...                             │
└─────────────────────────────────────┘
```

#### 工作流程
1. **命令接收**: `openyida dws <command>` → `bin/yida.js` 路由
2. **安装检测**: `dws-wrapper.js` 检查 dws 是否已安装
3. **自动安装**: 如未安装，提示并引导用户安装
4. **命令透传**: 使用 `spawn` 执行 dws 命令，继承 stdio
5. **错误处理**: 捕获并显示友好的错误信息

### 特性亮点

#### 1. 无缝集成
- 无需手动切换工具
- 统一的命令体验
- 一致的输出格式

#### 2. 智能安装
- 自动检测安装状态
- 交互式安装引导
- 支持跨平台（macOS/Linux/Windows）

#### 3. 友好提示
- 彩色输出
- 详细的帮助信息
- 常见问题指引

#### 4. AI Agent 友好
- JSON 输出格式 (`-f json`)
- 结构化错误响应
- MCP 协议支持

### 测试验证

运行集成测试：
```bash
node tests/dws-integration.test.js
```

预期输出：
```
开始测试钉钉 CLI 集成...

测试 1: dws --help
✓ 通过

测试 2: dws (无参数)
✓ 通过

测试 3: 主帮助包含 dws 命令
✓ 通过

测试 4: 示例命令在帮助中
✓ 通过

所有测试通过！✓
```

### 依赖说明

#### 新增依赖
- ❌ 无新增 NPM 依赖
- ✅ 使用系统安装的 dws 二进制文件

#### 系统要求
- Node.js >= 18 (OpenYida 已有要求)
- Go 1.21+ (仅构建时需要，用户无需安装)
- macOS / Linux / Windows

### 向后兼容性

- ✅ 完全向后兼容
- ✅ 不影响现有功能
- ✅ 不修改已有命令行为
- ✅ 可选安装和使用

### 已知限制

1. **需要企业管理员授权**
   - 钉钉 CLI 涉及企业数据访问
   - 需完成白名单配置

2. **凭证配置**
   - 需要钉钉应用 Client ID 和 Secret
   - 建议使用环境变量管理

3. **网络要求**
   - 需要访问钉钉开放平台 API
   - 首次安装需要访问 GitHub

### 后续计划

#### Phase 1 (已完成) ✅
- [x] 基础命令包装器
- [x] 自动安装功能
- [x] 帮助文档

#### Phase 2 (计划中)
- [ ] 快捷命令别名
- [ ] 常用场景脚本模板
- [ ] AI Agent Skills 集成
- [ ] MCP Server 配置生成

#### Phase 3 (未来)
- [ ] 批量操作工具
- [ ] 数据导出/导入
- [ ] 定时任务管理
- [ ] 监控告警集成

### 参考资料

- [钉钉 CLI 官方仓库](https://github.com/DingTalk-Real-AI/dingtalk-workspace-cli)
- [钉钉开放平台](https://open.dingtalk.com/)
- [MCP 协议规范](https://modelcontextprotocol.io/)
- [OpenYida 项目文档](../README.md)

### 贡献者

感谢以下项目的开源贡献：
- DingTalk-Real-AI/dingtalk-workspace-cli
- OpenYida 社区

### 许可证

遵循 OpenYida 项目的 MIT 许可证
